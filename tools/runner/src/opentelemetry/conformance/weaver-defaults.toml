# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0
#
# Findings filtered out for every domain. A domain's own config is appended to
# this file; a caller's `weaver.config` replaces both.

# Resource attributes the OTel SDK sets, which belong to no domain registry —
# see https://github.com/open-telemetry/weaver/issues/1456.
[[live-check.finding_filters]]
signal_type = "resource"
exclude_samples = [
  "service.name",
  "service.instance.id",
  "telemetry.sdk.language",
  "telemetry.sdk.name",
  "telemetry.sdk.version",
  "telemetry.auto.version",
]
